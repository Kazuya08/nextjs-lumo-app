NEXT_PUBLIC_TMDB_API_KEY=
NEXT_PUBLIC_TMDB_URL=
NEXT_PUBLIC_TMDB_URL_IMAGE=

TWITCH_TOKEN_URL=
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
IGDB_API_URL=

# Postgres (Neon) — conexão direta (migrations) e pooled (runtime)
DATABASE_URL=
DATABASE_URL_POOLED=

# Segredo para assinatura do JWT de sessão (ex.: openssl rand -base64 32)
AUTH_SECRET=

# Armazenamento de avatares (Vercel Blob) — gera em https://vercel.com → Storage → Blob
BLOB_READ_WRITE_TOKEN=
